program condicional;
var x, y : integer;
    z : integer;
begin
    read(x, y);
    write(x);
    writeln(x);
    for i := to do
        begin
            read(x, y);
        end;

    repeat
        read(x, y);
    until ( );

    while ( ) do
        begin
            read(x, y);
        end;

    if ( ) then
        begin   
            read(x, y);
        end;

    if ( ) then
        begin   
            read(x, y);
        end
    else
        begin   
            read(x, y);
        end;
end .

{ teste comentario  
program condicional;
var
    x, y: integer;
begin
    read(x);
    read(y);
    outro
    comentario 
    varias linhas 
    if (x >= 10) then
    begin
        write(x);
    end
    else
    begin
        write(y);
    end;
    comentario com codigo na frente  x := 10;
    
        end.}